# ==============================================================================
# EXAMPLE ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this file to .env and replace placeholder values with real credentials.
# DO NOT commit .env with real credentials to version control.
#
# ARCHITECTURE NOTES:
# - In production (Docker), some variables are hardcoded in Dockerfile
#   (IS_PRODUCTION, directory paths inside container, ANTHROPIC_API_URL)
# - Instance-specific variables are passed at container runtime via docker run -e
# - For local development, this .env file overrides Docker defaults
# ==============================================================================

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
PORT=8000
CLIENT_ORIGIN=http://localhost:3000

# Not set in local dev (defaults to falsy/false)
# Set to "true" in Dockerfile for production
# IS_PRODUCTION=true

# ------------------------------------------------------------------------------
# Directory Paths (Local Dev Override)
# ------------------------------------------------------------------------------
# These paths are correct for local development - no need to change
# In Docker: SANDBOX_DIR_PATH=/app/sandbox, ASSETS_DIR_PATH=/app/assets
SANDBOX_DIR_PATH=./_example_instance_dir/sandbox
SANDBOX_DIR_PATH_EXTERNAL=./_example_instance_dir/sandbox
SANDBOX_DIR_PATH_POSTGRES_INTERNAL=/app/_example_instance_dir/sandbox
ASSETS_DIR_PATH=./_example_instance_dir/assets

# ------------------------------------------------------------------------------
# Authentication (Instance-specific at Runtime)
# ------------------------------------------------------------------------------
# ⚠️  REPLACE WITH YOUR CLERK CREDENTIALS
# Get from: https://dashboard.clerk.com/
CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_CLERK_SECRET_KEY

# Optional: Enable open access (no authentication required)
# OPEN_ACCESS=1

# Optional: Bypass Clerk authentication for offline development
# Requires BYPASS_AUTH=true AND IS_PRODUCTION not set (or false)
# BYPASS_AUTH=true

# ------------------------------------------------------------------------------
# Instance Configuration (Instance-specific at Runtime)
# ------------------------------------------------------------------------------
INSTANCE_NAME="Local development site"
INSTANCE_REDIRECT_URL=http://localhost:3000

# Optional: Set language (en or fr, defaults to en)
# INSTANCE_LANGUAGE=fr

# Optional: Set calendar (gregorian or ethiopian, defaults to gregorian)
# INSTANCE_CALENDAR=ethiopian

# ------------------------------------------------------------------------------
# Database Configuration (Instance-specific at Runtime)
# ------------------------------------------------------------------------------
PG_HOST=localhost
PG_PORT=7001

# ⚠️  REPLACE WITH YOUR POSTGRES PASSWORD
PG_PASSWORD=your_postgres_password_here

# ------------------------------------------------------------------------------
# AI / External APIs
# ------------------------------------------------------------------------------
# API URL is hardcoded in Dockerfile for production
# In local dev, you can override it here if needed
ANTHROPIC_API_URL=https://api.anthropic.com/v1/messages

# ⚠️  REPLACE WITH YOUR ANTHROPIC API KEY (if using AI features)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-REPLACE_WITH_YOUR_ANTHROPIC_API_KEY

STATUS_API_KEY=example-key

# ------------------------------------------------------------------------------
# Deployment Metadata (Instance-specific at Runtime)
# ------------------------------------------------------------------------------
SERVER_VERSION=1.0.0
ADMIN_VERSION=1.0.0
DATABASE_FOLDER=development
