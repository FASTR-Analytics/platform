#!/bin/bash

# Protocol compliance sweep — companion to ./validate_queries and
# ./validate_migrations. Logic lives in validate_protocols.ts.

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

deno run --allow-read --allow-write="$SCRIPT_DIR/validate_protocols_baseline.json" "$SCRIPT_DIR/validate_protocols.ts" "$@"
