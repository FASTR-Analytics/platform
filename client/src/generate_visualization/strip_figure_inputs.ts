// P2: old strip/hydrate functions deleted (bundle carries everything).
// This file is kept to avoid breaking the import chain until all callers
// are updated; it exports nothing that callers still use.
