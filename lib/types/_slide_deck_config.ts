// =============================================================================
// Slide Deck Config — STORED SHAPE (slide_decks.config column)
//
// Stub schema for migration infrastructure. Replace with strict Zod schema
// when validation is needed. Type export deferred until schema is real.
// =============================================================================

import { z } from "zod";

export const slideDeckConfigSchema = z.unknown();
