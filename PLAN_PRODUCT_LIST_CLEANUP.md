# PLAN: Product list cleanup

Rework the Products tree list (`client/src/components/products/list_view.tsx`, `products.tsx`) so it reads as a designed file explorer rather than a flat grid of same-weight text. Track down every source of all-caps text in the list, whether a style or a stored label, and render the header row and every cell in sentence case. Give the name column the weight: `text-sm` in `text-base-content`, `font-700` for folders and `font-500` for products. Put every secondary column (Type, Package, Scope, Last updated, folder counts) and the header row in one quieter style, `ui-text-caption`, with the active sort column in `text-base-content`. Colour each product type's icon with its own theme accent, deck and report distinct, and keep chevrons muted, with no other colour beyond the kit's hover and focus row states. Give rows one height and one vertical rhythm, align the name cell's icon to its first text line, and rebalance column widths so Name takes the spare width and Type and Last updated never wrap their usual values. Remove folder colours across the stack: an idempotent migration drops `folders.color` (passing `./validate_migrations` and `./validate_fresh_boot`); `color` leaves `lib/api-routes/products/folders.ts`, `server/db/products/folders.ts`, the consolidation migration code and `server/tests/products_routes_test.ts`; `edit_folder_modal.tsx` becomes rename-only; `move_to_folder_modal.tsx` and `quickMoveFolder` stop sending colour; and the Valkey prefix for the folders payload is bumped if that payload is cached. SYSTEM_12 moves with the code, and the work is done when the floor in PROTOCOL_APP_PLANS is green, with this file deleted in that commit.
